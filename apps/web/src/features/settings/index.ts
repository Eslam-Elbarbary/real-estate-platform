export {
  getPlatformSettings,
  PLATFORM_SETTINGS_FALLBACK,
  resolveMetaDescription,
  resolveMetaTitle,
  resolveOgImage,
  resolveSiteName,
} from './get-platform-settings';
export type { PublicPlatformSettings } from './types';
