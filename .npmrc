# Local npm settings for this monorepo.
# strict-ssl=false is required on some Windows/corporate networks where
# TLS interception causes UNABLE_TO_VERIFY_LEAF_SIGNATURE during install.
strict-ssl=false
